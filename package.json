{
  "name": "junat.live",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "yarn --cwd site run dev",
    "test": "yarn vitest --run"
  },
  "workspaces": [
    "packages/*",
    "site"
  ]
}
