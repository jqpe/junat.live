{
  "name": "junat.live",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "start": "turbo run start",
    "test": "turbo run test"
  },
  "workspaces": [
    "packages/*",
    "site"
  ],
  "devDependencies": {
    "pm2": "^5.2.0",
    "turbo": "^1.2.8"
  }
}
