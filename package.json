{
  "name": "junat.live",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "start": "turbo run start",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@junat/prettier": "*",
    "turbo": "^1.2.14"
  },
  "prettier": "workspace:@junat/prettier"
}
