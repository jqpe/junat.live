{
  "name": "~digitraffic",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist/src/*",
    "dist/types/*"
  ],
  "scripts": {
    "build": "rimraf dist/* && tsc --build",
    "generate:fixtures": "yarn build && rimraf tests/fixtures/* && node dist/generators/fixtures.js",
    "pretest": "yarn build  && node  dist/generators/fixtures.js",
    "test": "vitest run",
    "test:watch": "node  node_modules/.bin/vitest",
    "coverage": "node  node_modules/.bin/vitest run --coverage"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "peerDependencies": {
    "node-fetch": "*"
  },
  "peerDependenciesMeta": {
    "node-fetch": {
      "optional": true
    }
  },
  "devDependencies": {
    "@vitest/ui": "^0.12.4",
    "c8": "^7.11.2",
    "node-fetch": "^3.2.4",
    "rimraf": "^3.0.2",
    "vitest": "^0.12.4"
  },
  "dependencies": {
    "core-js": "^3.22.4"
  }
}
