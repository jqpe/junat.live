{
  "name": "@junat/cms",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist/src/*",
    "dist/types/*"
  ],
  "scripts": {
    "build": "rimraf dist/* && tsc --build",
    "test": "vitest run",
    "test:watch": "node  node_modules/.bin/vitest",
    "coverage": "node  node_modules/.bin/vitest run --coverage"
  },
  "peerDependencies": {
    "node-fetch": "*"
  },
  "peerDependenciesMeta": {
    "node-fetch": {
      "optional": true
    }
  },
  "devDependencies": {
    "@vitest/ui": "^0.12.4",
    "c8": "^7.11.2",
    "nock": "^13.2.4",
    "rimraf": "^3.0.2",
    "vitest": "^0.12.4"
  }
}
